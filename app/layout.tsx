import type React from "react"
import type { Metadata } from "next"
import { Inter } from "next/font/google"
import "./globals.css"

const inter = Inter({ subsets: ["latin"] })

export const metadata: Metadata = {
  title: "CogniSync - AI-Powered Project Management",
  description: "Intelligent project coordination with AI agents for optimal team performance and project success.",
  keywords: ["project management", "AI", "team coordination", "productivity", "analytics"],
  authors: [{ name: "CogniSync Team" }],
  viewport: "width=device-width, initial-scale=1",
    generator: 'v0.dev'
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en" className="dark">
      <body className={inter.className}>{children}</body>
    </html>
  )
}
